"""
ai_service — Independent AI and RAG Service Package for ReadAndQues.

All external callers (Django backend, Data Pipeline) should only interact
with this package through the `ai_service.interface` module.
"""
