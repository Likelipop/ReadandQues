"""
NewsPipeline/sensors.py — Pipeline sensors (optional automated triggers).
"""

# Dynamic partition sensors are no longer needed because the pipeline
# uses built-in DailyPartitionsDefinition and daily_news_schedule.
